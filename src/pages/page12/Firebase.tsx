import SubmitFeedback from "./SubmitFeedback";
import Layout from "./Layout";

const Firebase = () => {
    return (
        <>
            <Layout>
                <SubmitFeedback />
            </Layout>
        </>
    );
}

export default Firebase;